apiVersion: v1
kind: Service
metadata:
  name: creativeiq-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: creativeiq-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
