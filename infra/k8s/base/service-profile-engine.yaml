apiVersion: v1
kind: Service
metadata:
  name: creativeiq-profile-engine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: creativeiq-profile-engine
  ports:
    - name: metrics
      port: 9101
      targetPort: 9101
