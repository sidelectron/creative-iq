apiVersion: v1
kind: Service
metadata:
  name: creativeiq-decomposition-worker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: creativeiq-decomposition-worker
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
