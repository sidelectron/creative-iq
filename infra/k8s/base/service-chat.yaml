apiVersion: v1
kind: Service
metadata:
  name: creativeiq-chat
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: creativeiq-chat
  ports:
    - name: http
      port: 8010
      targetPort: 8010
